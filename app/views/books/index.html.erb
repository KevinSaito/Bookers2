  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <table class='table'>

  <tr><%= current_user.profile_image %></tr>
  <tr>
	  <th>name</th>
	  <th><%= current_user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= current_user.introduction %></th>
  </tr>
  
</table>

<!--<%= form_with model: @user, local:true do |f| %>-->
  <!--<p><%= link_to 'Edit', edit_user_path %></p>-->
<!--<% end %>-->


  <h2>New book</h2>
  <%= form_with model: @book, local:true do |f| %>

    <h4>Title</h4>
    <%= f.text_field :title %>

    <h4>Opinion</h4>
    <%= f.text_area :body %>

    <%= f.submit 'Create Book' %>
  <% end %>
