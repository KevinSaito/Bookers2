<%= render 'books/list', book: @book, user: @user %>
<h1>Books</h1>
<table>
    <thead>

        <th>title</th>
        <th>opinion</th>

    </thead>
    <tbody>
    <% @books.each do |book| %>

      <tr>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
      </tr>

    <% end %>
    </tbody>
</table>